import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    // Personalizado
    primary: {
      main: "##00bdb2",

      // contrastText:"#fff"
    },
    secondary: {
      main: "#9ca3af",

      // contrastText:"#fff"
    },
  },
});

export default theme;
